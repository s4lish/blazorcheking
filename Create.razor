@page "/genre/create"
@inject IGenreRepository genreRepository
@inject NavigationManager navi
@inject IOperation operation
<h3>CreateGenre code is : @GuidNew</h3>

<GenreForm genre="genre" OnValidSbt="Create">

</GenreForm>



@code {
    private Genre genre = new Genre();
    private string GuidNew;
    protected override void OnInitialized()
    {

        base.OnInitialized();
    }

    private async Task Create()
    {
        Console.WriteLine("Method Created.");
        Console.WriteLine(genre.Name);

        try
        {
            //await genreRepository.CreateGenre(genre);
            GuidNew = await operation.ReturnGNewGuid(genre);
            //navi.NavigateTo("genre/Index");
        }
        catch (Exception ex)
        {
            //...
            Console.WriteLine(ex.Message);
        }
    }
}
